<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Data Sheet Provi_spa</title>
<style>
  body {
    font-family: "Segoe UI", Arial, sans-serif;
    background-color: #f6f8fa;
    margin: 0;
    padding: 20px;
  }

  h1 {
    text-align: center;
    color: #2e7d32;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    background: #fff;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 3px 8px rgba(0,0,0,0.05);
  }

  th {
    background: #2e7d32;
    color: white;
    padding: 10px;
    text-align: center;
  }

  td {
    border: 1px solid #eee;
    padding: 8px;
    text-align: center;
  }

  tr:nth-child(even) { background: #f9f9f9; }
  tr:hover { background: #e8f5e9; }

  .footer {
    text-align: center;
    color: #777;
    margin-top: 20px;
    font-size: 0.9em;
  }
</style>
</head>
<body>

<h1>📄 Data dari Sheet: Provi_spa</h1>
<table id="data"></table>

<div class="footer">Data diambil langsung dari Google Spreadsheet</div>

<script>
const sheetID = "1u7sHZTiuVvw9l1OHethOqi2lGhO-9c09ieN2Rvwye1w"; // ID spreadsheet kamu
const sheetName = "Provi_spa"; // nama sheet
const url = `https://docs.google.com/spreadsheets/d/${sheetID}/gviz/tq?tqx=out:csv&sheet=${sheetName}`;

fetch(url)
  .then(res => res.text())
  .then(csv => {
    const rows = csv.split("\n").map(r => r.split(","));
    const table = document.getElementById("data");

    // buat header
    const thead = document.createElement("thead");
    const trHead = document.createElement("tr");
    rows[0].forEach(h => {
      const th = document.createElement("th");
      th.textContent = h.replace(/(^"|"$)/g, "");
      trHead.appendChild(th);
    });
    thead.appendChild(trHead);
    table.appendChild(thead);

    // isi tabel
    const tbody = document.createElement("tbody");
    rows.slice(1).forEach(r => {
      if (r.join("").trim() === "") return;
      const tr = document.createElement("tr");
      r.forEach(v => {
        const td = document.createElement("td");
        td.textContent = v.replace(/(^"|"$)/g, "");
        tr.appendChild(td);
      });
      tbody.appendChild(tr);
    });
    table.appendChild(tbody);
  })
  .catch(err => {
    document.getElementById("data").innerHTML = `<tr><td>Error: ${err}</td></tr>`;
  });
</script>

</body>
</html>
